{
  "name": "quant-report-site",
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "dev": "npm run prepare && astro dev",
    "start": "npm run dev",
    "prepare": "python ../prepare_site.py",
    "build": "npm run prepare && astro build",
    "build:astro": "astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/tailwind": "^5.0.0",
    "astro": "^5.0.0",
    "tailwindcss": "^3.4.0"
  }
}
